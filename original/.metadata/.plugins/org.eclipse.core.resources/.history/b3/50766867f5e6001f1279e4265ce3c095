<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ユーザー削除</title>
    <!-- BootstrapのCSSを読み込む -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <form action="/auth/delete" method="post">
    <p>本当に削除してもよろしいでしょうか？</p>
    <p>削除を実行するには「削除」と入力してください。</p>
    <input type="text" id="deleteConfirmInput" class="form-control" placeholder="削除">
    
    <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteConfirmModal">
        ユーザーを削除
    </button>

    <!-- 削除確認モーダル -->
    <div class="modal fade" id="deleteConfirmModal" tabindex="-1" aria-labelledby="deleteConfirmModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="deleteConfirmModalLabel">削除の確認</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="閉じる"></button>
                </div>
                <div class="modal-body">
                    <p>本当に削除してもよろしいでしょうか？</p>
                    <p>削除を実行するには「削除」と入力してください。</p>
                    <input type="text" id="deleteConfirmModalInput" class="form-control" placeholder="削除">
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">キャンセル</button>
                    <button type="submit" class="btn btn-danger" id="confirmDeleteButton" disabled>削除</button>
                </div>
            </div>
        </div>
    </div>
</form>

<script>
    document.getElementById("deleteConfirmModalInput").addEventListener("input", function () {
        let confirmButton = document.getElementById("confirmDeleteButton");
        if (this.value === "削除") {
            confirmButton.removeAttribute("disabled");
        } else {
            confirmButton.setAttribute("disabled", "true");
        }
    });
</script>
</body>
</html>